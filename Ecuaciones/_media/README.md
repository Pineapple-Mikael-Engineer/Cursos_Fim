# _media — figuras y código del curso Ecuaciones

Convención de la vault:

- `img_gen/` — SVG finales que se embeben en las notas (`![[nombre.svg|ancho]]`).
- `code_gen/` — fuentes que los generan: `*.py` (matplotlib, paleta Ocean Forest) y `tikz/*.tex` (diagramas B/N).

Generación con la skill `graficar-figuras`. Regla de oro: renderizar SVG→PNG y *verla* antes de dar por buena la figura.
